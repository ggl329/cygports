category: Python
requires: python39
sdesc: "Simple program which checks Python source files for errors"
ldesc: "Pyflakes analyzes programs and detects various errors. It works by parsing the source file, not importing it, so it is safe to use on modules with side effects. It's also much faster."
external-source: python-pyflakes

